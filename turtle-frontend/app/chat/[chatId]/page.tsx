import { useEffect } from "react";

export default function({ params }: {
  params: {
    projectId: string
  }
}) {
  const projectId = params.projectId;

  useEffect(() => {

  }, [])
  return <div>

  </div>

}
